#include <bits/stdc++.h>

#define forn(i,n) for(int i=0;i<n;i++)
#define nl '\n'

using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
        cout << sizeof (double) << nl;
        cout << sizeof (long double) << nl;
}

